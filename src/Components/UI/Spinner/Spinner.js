import classes from './Spinner.module.css'


const spinner = () => {
    return (
        <div className={classes.Container} >
            <div className={classes.loader} >sddssd</div>
        </div>
    )
}

export default spinner